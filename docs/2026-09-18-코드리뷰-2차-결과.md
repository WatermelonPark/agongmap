# 코드리뷰 2차 결과 (2026-09-18, 리뷰 세션)

1차 리뷰(09-16) 이후 커밋 46건, 약 2,000줄을 봤다. 09-17에 각 세션이 고친 것이 **실제로 그 결함을
막는지**를 워크트리에서 변이로 재확인하는 데 비중을 뒀다. 높음 등급은 없다. 확정 15건 + 낮음 6건 중
리뷰 세션이 2건을 고쳤고(37687d97), 나머지는 백로그 19~23번에 등재했다. 테스트 418 → 423.

## 09-17 수정은 닫혔는가

| 수정 | 닫혔는가 | 확인 |
|---|---|---|
| 퀴즈 시험 2건 (00858331) | 예 | `qClear()` 제거·섞기 제거·섞기를 `Math.random`으로 → 전부 빨강 |
| 인허가 시험 (b8c0b2bd) | 예(단, 아래 21번) | 12개월 합으로 되돌리면 빨강. 새 관계의 여유 0.578 대 1.303 |
| 낮음 묶음 개발 (58462c8b) | 예 | 시험이 생성기 반올림을 쓰지만 node 로 사이트 JS 와 따로 대조해 헛돌지 않는다 |
| 캡션 기준월 (278b0b25) | 예(연결부 공백은 20번) | 캡션 재하드코딩 → 빨강 |
| 도구 실행 시험 (b67926cf) | 예 | 순환 import 빨강, 전체 시험 뒤 작업 트리 깨끗 |
| 이론 3편 가드 (cc16a7bc) | 부분 → 37687d97 로 닫음 | 가드는 요란하게 멈추나 기존 초안이 0바이트가 됐다 |

알림 개편(b5045c31)은 배치 종료 상태 16가지를 전부 따졌다. **실패인데 코멘트 없이 끝나는 경로는 현재
코드에 없다.** 다만 그 경로가 끊겨도 시험이 초록인 빈틈이 셋 있다(20번).

## 리뷰 세션이 고친 것 (37687d97)

- **초안을 다 만든 뒤에 쓴다.** `make_theory_post.py`·`make_naver_post.py` 세 곳이 파일을 먼저 열고
  render 를 돌렸다. 3편 생성 가드가 걸리자 사람이 해석을 채운 초안이 18,140 → 0바이트가 됐다(`drafts/`는
  gitignore 라 복구 불가). `write_draft`로 모으고 임시 파일에 쓴 뒤 바꿔치기한다.
- **도구 실행 시험을 전 도구·전 예외로.** 리뷰 세션이 09-12 에 넣은 시험이다. `except Exception: pass`가
  ImportError 외의 최상단 실패를 삼켰고, 대상이 손 목록 12개라 배치가 부르는 도구 8개가 빠져 있었다.
  `tools/*.py`에서 가드 있는 41개를 파생한다(약 8초). 변이 6종 확인.

## 백로그 19 — 발행 도구 (마케팅)

1. `make_naver_post.py:718` **(중간, 사실 오류가 나간다)** `thumb_message`는 "고점이 3개월보다 오래됐고
   하락 3% 이상"일 때만 하락 문구를 쓰고 나머지를 전부 "지금이 역대 최고가"로 처리한다. 고점이 24개월
   전이고 현재 −1.7%여도 그렇게 찍힌다. 전북 −4%·울산 −5%·충북 −6%가 문턱 근처다. 시험은 단조 상승
   픽스처만 다룬다.
2. `:1012` 생성기를 다시 돌리면 `--thumb-msg`로 만든 맞춤 썸네일을 기본 문구로 덮는다. HTML 초안은
   손댄 흔적이 있으면 보존하는데 썸네일은 그 판정 앞에서 쓰인다.
3. `:94-112` `_published_zone_posts`(제목 매칭·카테고리 필터)를 지나는 시험이 없다. `test_post_rotation`은
   전부 `published=`를 주입한다. `ZONE_CAT`은 `close_published_issues.KIND_TO_CATEGORY`의 문자열을 손으로
   복제했다 — 매칭 문자열·필터·값을 바꿔도 418개 전부 초록.
4. `:139-145` RSS 가 응답은 하는데 글이 0건이면 `published`가 `{}`라 경고 분기(`is None`)를 안 타고,
   `.rotation.json`까지 없으면 경고 없이 1번 지역(서울, 08-16 기발행)부터 다시 만든다.
5. `:108` 제목에 지역명이 둘이면 같은 길이 이름을 set 순회 순서로 맞춰 봐서 실행마다 집계 지역이 달라진다
   (`PYTHONHASHSEED` 1~6 에서 부산 3회·서울 3회). '세종시 아파트'처럼 접미사가 붙으면 못 센다.
6. `test_theory_sync_claims.py:32` 이미 발행한 3편의 주장을 실데이터로 단정하는 시험이 매일 배치 게이트에
   있다(서울 0.55 대 전북 0.63, 여유 0.08). 재산정에서 최저 지역이 바뀌면 "발행본은 고치지 않는다"는
   결정과 맞물려 데이터 커밋이 막힌다. 생성 가드는 두고 이 단정은 게이트 밖으로.

## 백로그 20 — 알림·뒤처짐 (데이터)

1. `month_lag.py:96-104` **(중간, 10월 초부터 매일 메일)** 뒤처짐을 "가장 빠른 계열(금리·월간 시세) 대비"로
   재서 계열별 정상 공표 시차가 그대로 뒤처짐이 된다. 미분양·규모별은 지금도 2개월 차이이고
   `check_freshness`는 기본통계 지연을 약 95일까지 정상으로 본다. 금리가 10월 초 2026.09 가 되는 며칠
   동안 차이가 3개월이 되어 `ESCALATE_MONTHS`에 걸린다. 계열별 허용 시차를 `check_freshness`의 GRACE 와
   같은 상수에서 읽을 것.
2. `format_batch_report.py:196` "0/3 재시도 대기"와 "3/3 소진"의 제목이 같다(🔴 데이터 갱신 실패).
   스로틀 날 최대 4통(시도 1·2·3 + Run failed)이고 회복돼도 평일엔 회복 메일이 없다.
3. 시험 공백 셋(전부 변이로 초록 확인):
   - `test_mail_volume.py:60` — 워크플로 호출에서 `--meta _report.meta`를 빼면 모든 실패가 "정상 회차"로
     `exit 0` 된다. `SEND` 게이트를 `&& false`로 무력화해도 초록.
   - `update_adv_data.py:1701`·`update-cloud.yml:172` — main 의 `write_supply_stalled()` 호출을 지우거나
     `include-hidden-files`를 false 로 바꿔도 초록. 09-08~12 와 같은 정지가 다시 ✅로 끝난다.
   - `update-cloud.yml:467` — `rep "$LAG"`를 `true`로 바꾸거나 `month_lag`·`batch_notes`가 아무것도 안 찍어도
     초록. `month_lag` 호출 줄은 어떤 시험도 안 본다.
4. 낮음: `format_batch_report.py:200·254` 장기 뒤처짐 회차에서 제목(🟡)과 본문 머리(✅ 정상)가 다른 말을
   한다 · `:198·241` "➖ 커밋 없음 + ⚠️ 공급 멈춤"이 "데이터는 갱신됨 · 곁가지 하나"로 나간다 ·
   `month_lag.line`과 `lag_notice` 사이 줄 형식 계약을 잇는 시험이 없다(구분자 변이 초록) ·
   `.fetch_failed`(비핵심 원천 지속 실패)가 어떤 기록에도 안 오른다 · `refresh_cycle_data.py:196`
   `build_jratio`의 `prd`가 `jratio_prose`로 넘어가는 연결을 보는 시험이 없다(백로그 17 의 원래 증상이
   재발해도 초록).

## 백로그 21 — 배치 게이트의 실데이터 단정 (데이터)

`test_sido_zones.py:283-286` 관계 부등식은 풀렸지만 `not by['충남']['pwarn']`이 남았다. 충남 pbr 은
2026.01 1.62 → 2026.07 1.30 으로 6개월에 0.32 내렸고, 같은 기울기로 6~7개월 가면 경고 컷 0.95 아래가
되어 매일 배치가 "인허가 계열 오염 의심"으로 막힌다. 대구는 12개월에 1.03 → 0.53 을 기록한 적이 있다.

## 백로그 22 — 로컬 러너 서술과 bat (PM·대표 결정)

- **CLAUDE.md "실운영은 로컬 러너가 맡는다"는 사실이 아니다.** 마지막 로컬 커밋과 `logs/` 기록이 모두
  2026-07-23 이고 작업 스케줄러 항목은 다음 실행 시간이 N/A 다. 실제 배포 경로는 클라우드 배치 하나다.
  1차 리뷰 10번·백로그 11번·`test_batch_parity` 독스트링이 이 전제로 쓰였다. 로컬 러너를 다시 켤지에
  따라 문구가 달라지므로 결정이 필요하다.
- `run_weekly_update.bat:151` 새 pytest 게이트(또는 그 뒤 단계)가 실패하면 이미 다시 쓴 생성 파일 약
  28개를 공유 작업 트리에 남기고 끝난다. `git commit`은 경로 지정 없이 인덱스 전체를 커밋하고(남이
  스테이징한 파일이 실린다), 94행은 새 규약이 금지한 `--autostash`를 쓴다. 휴면이라 잠복 상태.

## 백로그 23 — 발행 확인 이슈 (마케팅)

`close_published_issues.py:191-198` 제목을 "✅ 발행 확인됨"으로 먼저 바꾸고 닫는데, 닫기가 일시 실패하면
다음 회차에 `parse_title`이 None 이라 그 이슈가 영구히 열린 채 남는다 · `:141-147` `gh issue view`가
실패하면 old 가 빈 문자열이 되어 점검 체크리스트 본문을 기록문으로 덮는다 · `:171-173` 매칭 로직(예정일·
카테고리 조건)에 시험이 없다 — 예정일 조건을 빼면 지난주 주간 글이 이번 주 이슈를 닫는데 초록이다.

## 확인했고 이상 없던 것

- 공급 멈춤은 "새 달이 아직 없는 회차"에서 오발동하지 않는다. `.supply_stalled`는 gitignore·TARGETS 밖·
  새 체크아웃이라 잔존하지 않는다. `check_freshness` 개수 게이트 불변식이 전 경로에서 성립한다.
- `month_lag`는 오늘 날짜·요일을 쓰지 않아 러너 시각대와 무관하고, 연도 경계·반기·빈 값 처리가 맞다.
- 썸네일 폰트는 저장소 동봉 서브셋이고 Windows 경로를 박지 않았다. `THUMB_GROUP`은 `sido_zones.ORDER`와
  차집합이 없다. 104px 에서 16개 지역 기본 문구가 1,080px 안에 든다. 산출물은 전부 `drafts/` 안이다.
- 대결 수락 이벤트의 중복 방지 키는 세트:점수:시드라 새 링크가 누락되지 않고, 저장소 예외가 boot 를
  깨지 않는다.
- 대상 시험 파일 18개는 저장소 안에 쓰지 않고 네트워크·오늘 날짜에 기대지 않는다.
